package com.flowstate.domain;

/**
 * The three buckets used by the 50/30/20 budgeting benchmark
 * (Elizabeth Warren's "All Your Worth", 2005 — the external, well-established
 * rule FlowState's budget recommendations are validated against).
 */
public enum CategoryGroup {
    NEEDS,
    WANTS,
    SAVINGS_AND_DEBT,
    INCOME
}
