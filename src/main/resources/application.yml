spring:
  application:
    name: flowstate
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:h2:file:./data/flowstate;AUTO_SERVER=TRUE}
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: ${PORT:8080}

logging:
  level:
    com.flowstate: INFO
